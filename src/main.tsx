import React from "react";
import ReactDOM from "react-dom/client";
import Page from "./pages/ai-portal";
import "./index.css"; // ok if this file doesn't exist
ReactDOM.createRoot(document.getElementById("root")!).render(<Page />);

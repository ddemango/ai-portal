{
  "name": "ai-portal",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --port $PORT --host",
    "build": "vite build",
    "preview": "vite preview --port $PORT --host"
  },
  "dependencies": {
    "react": "latest",
    "react-dom": "latest",
    "react-helmet": "^6.1.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "latest",
    "typescript": "latest",
    "vite": "latest"
  }
}

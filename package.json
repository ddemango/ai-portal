{
  "name": "ai-portal",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --port $PORT --host",
    "build": "vite build",
    "preview": "vite preview --port $PORT --host"
  },
  "dependencies": {
    "react": "latest",
    "react-dom": "latest"
  },
  "devDependencies": {
    "vite": "latest",
    "@vitejs/plugin-react": "latest",
    "typescript": "latest"
  }
}
